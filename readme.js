# 🌟 Star Bot - Multi-Session WhatsApp Bot  

Star Bot is a **WhatsApp Web bot** that supports **multiple independent sessions**. Each user can start their own session, link their phone number, and use the bot without affecting others.  

---

## 🚀 Features  
✅ Multi-session support (each user has a separate session)  
✅ Link and verify phone numbers with pairing codes  
✅ Start and manage sessions independently  
✅ Logout and delete sessions easily  
✅ Check active sessions and statuses  

---

## 📌 Commands  
| Command | Description |
|---------|-------------|
| `!start <ID>` | Start a new session for a user |
| `!status <ID>` | Check if a session is active |
| `!logout <ID>` | End a session and delete its data |
| `!sessions` | List all active sessions |
| `!help` | Show all available commands |
| `!ping` | Check if the bot is online |

---

## 📥 Installation  

### **1. Install Node.js & Dependencies**  
Make sure you have **Node.js** installed. Then, run:  

```sh
npm install
